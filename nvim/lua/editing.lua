-- ts-comments.nvim — treesitter-aware commenting (integrates with native gc/gcc)
require("ts-comments").setup()

-- grug-far.nvim — multi-file search and replace
require("grug-far").setup({
  headerMaxWidth = 80,
})
